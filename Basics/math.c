#include <stdio.h>
#include <math.h>

int main()
{
  printf("%f \n", pow(2, 2)); // 2 to the power 2 = 4
  printf("%f \n", sqrt(81)); // square root of 81 = 9
  printf("%f \n", ceil(36.356)); // round up to 37
  printf("%f \n", floor(36.656)); // round down to 36
  return 0;
}